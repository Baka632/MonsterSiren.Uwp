<Page
    x:Class="MonsterSiren.Uwp.Views.PlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animations="using:Microsoft.Toolkit.Uwp.UI.Animations"
    xmlns:app="using:MonsterSiren.Uwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Baka632.Library.Uwp.CSharp.Helpers"
    xmlns:local="using:MonsterSiren.Uwp.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:menuFlyouts="using:MonsterSiren.Uwp.Controls.MenuFlyouts"
    xmlns:models="using:MonsterSiren.Uwp.Models"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:playlistModels="using:MonsterSiren.Uwp.Models.Playlists"
    xmlns:services="using:MonsterSiren.Uwp.Services"
    xmlns:viewmodels="using:MonsterSiren.Uwp.ViewModels"
    xmlns:wctControls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    x:Name="Self"
    d:DataContext="{d:DesignInstance Type=viewmodels:PlaylistViewModel}"
    mc:Ignorable="d">
    <Page.Resources>
        <menuFlyouts:PlaylistMenuFlyout
            x:Name="PlaylistContextFlyout"
            x:FieldModifier="public"
            SourceData="{x:Bind ViewModel.SelectedPlaylist, Mode=OneWay}"
            StartMultipleSelectionCommand="{x:Bind ViewModel.StartMultipleSelectionCommand}" />

        <menuFlyouts:PlaylistListMenuFlyout
            x:Name="PlaylistSelectionFlyout"
            x:FieldModifier="public"
            DeselectAllListCommand="{x:Bind ViewModel.DeselectAllSongListCommand}"
            SelectAllListCommand="{x:Bind ViewModel.SelectAllSongListCommand}"
            SourceData="{x:Bind ViewModel.PlaylistFactory, Mode=OneWay}"
            StopMultipleSelectionCommand="{x:Bind ViewModel.StopMultipleSelectionCommand}" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,10"
            Orientation="Horizontal">
            <FontIcon
                Margin="0,0,10,0"
                HorizontalAlignment="Center"
                FontSize="40"
                Glyph="&#xE14C;" />
            <TextBlock
                x:Uid="PlaylistTextBlock"
                Margin="0,0,5,0"
                HorizontalAlignment="Center"
                FontSize="30" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel
                Grid.Row="0"
                Margin="0,0,0,5"
                Orientation="Horizontal">
                <Button
                    AccessKey="0"
                    Background="Transparent"
                    Command="{x:Bind ViewModel.CreateNewPlaylistCommand}">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon
                            Margin="0,0,5,0"
                            HorizontalAlignment="Center"
                            Glyph="&#xE109;" />
                        <TextBlock x:Uid="PlaylistPage_NewPlaylist" HorizontalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <GridView
                x:Name="PlaylistGridView"
                Grid.Row="1"
                animations:Connected.ListItemElementName="PlaylistCoverImage"
                animations:Connected.ListItemKey="PlaylistItemToDetailAnimationKey"
                animations:ReorderGridAnimation.Duration="500"
                x:FieldModifier="Internal"
                CanDragItems="True"
                DragItemsStarting="OnPlaylistItemsDragStarting"
                IsItemClickEnabled="True"
                ItemClick="OnPlaylistItemClick"
                ItemsSource="{x:Bind services:PlaylistService.TotalPlaylists}"
                SelectionMode="None"
                Visibility="{x:Bind helpers:XamlHelper.ReverseVisibility(IsTotalPlaylistEmpty), Mode=OneWay}">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="playlistModels:Playlist">
                        <Grid
                            Padding="5"
                            Background="Transparent"
                            ContextFlyout="{Binding ElementName=Self, Path=ViewModel.SelectedPlaylistContextFlyout, Mode=OneWay}"
                            DataContext="{x:Bind}"
                            RightTapped="OnGridViewItemRightTapped">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>

                            <wctControls:ImageEx
                                x:Name="PlaylistCoverImage"
                                Grid.Row="0"
                                Width="170"
                                Height="170"
                                PlaceholderSource="ms-appx:///Assets/NullPlaylist.png"
                                PlaceholderStretch="UniformToFill"
                                Source="{x:Bind PlaylistCoverImageUri, Mode=OneWay}"
                                Stretch="UniformToFill" />

                            <TextBlock
                                Grid.Row="1"
                                Margin="0,5,0,0"
                                HorizontalAlignment="Left"
                                FontWeight="SemiBold"
                                Text="{x:Bind Title, Mode=OneWay}" />
                            <TextBlock
                                Grid.Row="2"
                                Margin="0,0,0,5"
                                HorizontalAlignment="Left"
                                Foreground="#8f8f8f"
                                Text="{x:Bind x:String.Format(app:CommonValues.SongCountFormat, SongCount), Mode=OneWay}"
                                TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{x:Bind helpers:XamlHelper.ToVisibility(IsTotalPlaylistEmpty), Mode=OneWay}">
                <FontIcon FontSize="60" Glyph="&#xE955;" />
                <TextBlock x:Uid="NoPlaylistTextBlock" FontSize="20" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
